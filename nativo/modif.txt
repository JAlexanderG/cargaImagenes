    using (var inputStream = image.OpenReadStream())
    using (var originalImage = new Bitmap(inputStream))
    using (var grayscaleImage = new Bitmap(originalImage.Width, originalImage.Height, PixelFormat.Format8bppIndexed)) // 🔥 Convertir a 8bpp
    using (var gfx = Graphics.FromImage(grayscaleImage))
    using (var bmpMemoryStream = new MemoryStream())
    {
        gfx.Clear(Color.White);
        gfx.DrawImage(originalImage, 0, 0, originalImage.Width, originalImage.Height);

        grayscaleImage.Save(bmpMemoryStream, ImageFormat.Bmp);
        imageData = bmpMemoryStream.ToArray();
    }